spring.application.name=lms-backend

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/foliopath360_lms
spring.datasource.username=root
spring.datasource.password=123456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.type.preferred_uuid_jdbc_type=char

# SQL Initialization
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true  

# JWT Configuration
jwt.secret=384E574F6B53414D79516F504C4B3237394D41564852364A5334303256484555
jwt.access-token-expiration=3600000

# Super Admin Bootstrap (override via environment variables in production)
super-admin.email=${SUPER_ADMIN_EMAIL:admin@foliopath360.com}
super-admin.password=${SUPER_ADMIN_PASSWORD:Admin@12345}
super-admin.first-name=${SUPER_ADMIN_FIRST_NAME:System}
super-admin.last-name=${SUPER_ADMIN_LAST_NAME:Administrator}

# ===============================
# Mail / SMTP Configuration
# ===============================

app.mail.enabled=true
app.mail.from=shivashankar.damidi@gmail.com

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=balakrishnasripathi143@gmail.com
spring.mail.password=utyfusmmnztfvthb

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

spring.mail.default-encoding=UTF-8
spring.mail.test-connection=false

# App links sent in emails
app.staff-setup.link-base-url=${STAFF_SETUP_LINK_BASE_URL:http://localhost:5173/set-password}

# Captcha / OTP settings
app.captcha.expiry-minutes=5
app.otp.expiry-minutes=10

# ===============================
# Razorpay Payment Gateway
# Get test keys from the Razorpay Dashboard (Test mode).
# NEVER put the key secret in frontend code.
# ===============================
razorpay.key-id=${RAZORPAY_KEY_ID:}
razorpay.key-secret=${RAZORPAY_KEY_SECRET:}
razorpay.webhook-secret=${RAZORPAY_WEBHOOK_SECRET:}
razorpay.currency=${RAZORPAY_CURRENCY:INR}

# Server Configuration
server.port=8081

# ===============================
# Code Execution (student coding playground)
# Override via env vars, e.g. CODE_EXECUTION_TIMEOUT_MS, CODE_EXECUTION_NODE_PATH
# ===============================
code.execution.timeout-ms=${CODE_EXECUTION_TIMEOUT_MS:5000}
code.execution.max-output-bytes=${CODE_EXECUTION_MAX_OUTPUT_BYTES:1048576}
code.execution.temp-dir=${CODE_EXECUTION_TEMP_DIR:${java.io.tmpdir}}
code.execution.javac-path=${CODE_EXECUTION_JAVAC_PATH:javac}
code.execution.java-path=${CODE_EXECUTION_JAVA_PATH:java}
code.execution.python-path=${CODE_EXECUTION_PYTHON_PATH:python}
code.execution.node-path=${CODE_EXECUTION_NODE_PATH:node}
